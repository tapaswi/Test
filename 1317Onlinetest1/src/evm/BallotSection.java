package evm;

public class BallotSection{
	TotalButton totalButton = new TotalButton();
	BallotButton ballotButton = new BallotButton();
	
	public Boolean totalButton(){
		if(totalButton.pressed()==true)
			
		return false;
		
	}
}
